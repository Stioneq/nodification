/*
    Write the function that will takes collection of async functions and invokes them in parallel with limit
    Each function should be called with callback param.
    When sequence will be finished, it finish callback should invoked
    
*/

function parallelExecute(collection, concurrent, callback, finish){
    
}