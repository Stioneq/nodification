/*
    Implement task queue class, that allows us to execute task in parallel
    It should contain concurrency field that defines how many tasks can be executed in parallel
    
*/

class TaskQueue{
    constructor(concurrent){
        
    }
}